#!/bin/bash

BASE=${1%.*}
NUM=1
while [ -e "$BASE-$NUM.svg" ] ; do
	NUM=$((NUM + 1))
done

cp "$HOME"/skola/build/empty.svg "$BASE-$NUM.svg"
git add "$BASE-$NUM.svg"
echo "\\svgilust{$BASE-$NUM}"
inkscape "$BASE-$NUM.svg" >/dev/null 2>&1 &
